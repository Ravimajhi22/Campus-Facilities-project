package com.campusFacilities.www.service.Imp;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.campusFacilities.www.model.Transport.Bus;
import com.campusFacilities.www.repository.TransportRepository;
import com.campusFacilities.www.service.TransportService;

@Service
public class TransportServiceImpl implements TransportService {

    @Autowired
    private TransportRepository transportRepository;

    @Override
    public Bus addBus(Bus bus) {
        return transportRepository.save(bus);
    }

    @Override
    public Bus addBus(Long busNum, String driverName, String contact) {
        Bus bus = new Bus();
        bus.setBusNum(busNum);
        bus.setDriverName(driverName);
        bus.setContact(contact);
        return transportRepository.save(bus);
    }

    @Override
    public List<Bus> getAllBuses() {
        return transportRepository.findAll();
    }
}
